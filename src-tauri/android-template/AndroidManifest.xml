<?xml version="1.0" encoding="utf-8"?>
<!--
  CryptoNote - AndroidManifest.xml
  Committed template — CI copies this after 'tauri android init'
  to ensure biometric permissions are always present.
  Package: com.cryptonote.desktop
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryptonote.desktop">

    <!-- Network access for cloud sync -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Biometric authentication (fingerprint / face unlock) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:name="com.cryptonote.desktop.MainActivity"
            android:label="@string/main_activity_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
