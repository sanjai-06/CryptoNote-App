# Git ignore for CryptoNote

# Rust
target/
Cargo.lock

# Node
node_modules/
dist/
server/dist/
server/node_modules/

# Environment
.env
server/.env

# Tauri
src-tauri/gen/

# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.idea/
.vscode/
*.code-workspace

# Vault / keys (NEVER commit)
*.db
*.key
*.pem
*.crt
*.p12

# Terraform (state contains plaintext secrets + credentials)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.lock.hcl

# Kubernetes secrets (only the .template file is committed)
k8s/secrets.yaml
